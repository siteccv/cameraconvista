# REPLIT AGENT — SITE CCV / EVENTI PRIVATI — SOLO ANALISI (NO MODIFICHE)

## Contesto
Stiamo lavorando al progetto SITE CCV. Dobbiamo implementare la feature “Eventi Privati” seguendo ESATTAMENTE le specifiche nel file:
- REPORT_EVENTI_PRIVATI_SITE_CCV_UPDATED_v2.md (allegato in chat)

## Regola assoluta (in questa fase)
✅ Devi SOLO analizzare il file MD e il progetto attuale.
❌ NON devi modificare codice, non creare file, non fare commit, non cambiare config.
Output richiesto: un piano di implementazione modulare, prioritizzato, a step (uno dopo l’altro).

## Obiettivo funzionale (riassunto)
- Pagina “Eventi Privati” (oggi nascosta) con 3 card:
  1) Aperitivo
  2) Cena
  3) Evento Privato Esclusivo
- Click su card → pagina dedicata con info + foto (configurabile da Admin con sezioni testo/foto).
- Da ogni pagina dedicata si avvia un wizard UNICO (stesso wizard per tutte le tipologie, parametrico).
- Fine wizard → invio di UNA sola email “classica” a info@cameraconvista.it con dati già filtrati e leggibili.
- Dopo l’invio: il sito non deve più essere coinvolto.
  - NESSUN archivio
  - NESSUN codice richiesta
  - NESSUNA email al cliente (solo conferma on-site)

## Vincoli tecnici e di governance
- Architettura MODULARE, scalabile, senza duplicazioni.
- Wizard unico parametrico (no tre wizard duplicati).
- Endpoint unico per l’email (no endpoint multipli per tipologia).
- Evitare logiche parallele/fallback ambigui: convergere verso una singola implementazione canonica.
- Non compromettere il codice esistente: ogni step deve essere “safe” e isolato.
- Pagine dedicate e contenuti devono essere gestibili da Admin con sezioni modulari testo/foto (come da specifica).
- Invio email via Resend dal backend: HTML + fallback testo.
- From = info@cameraconvista.it (provvisorio tecnico) + Reply-To = email cliente.
- Oggetto email: “Richiesta evento - Nome Cognome - Tipologia - Giorno Mese” (mese in italiano, senza orario e senza anno).
- Termini/Condizioni: modale + checkbox unica obbligatoria.
- Wizard: 7 step con campi obbligatori, spunte “indicativo”, note facoltative, nessun blocco su range persone.

## Cosa devi produrre adesso (output in chat)
1) **Mappa impatto**: dove sta/sta per stare la pagina Eventi Privati, come funziona oggi la navigazione, admin content system, backend mail attuale (se esiste).
2) **Proposta architetturale** coerente col codice esistente:
   - componenti (UI) riusabili
   - wizard parametrico
   - gestione contenuti admin per 3 pagine dedicate (sezioni testo/foto)
   - endpoint backend unico per invio email
3) **Piano a step prioritizzati** (step 1, step 2, step 3…):
   - Ogni step deve essere “piccolo”, verificabile, e non rompere nulla.
   - Per ogni step: scopo, file coinvolti (solo lista), rischi, test rapidi da fare.
   - Dopo ogni step: indicare cosa devo riportarti in chat (es. screenshot, log, pagina raggiungibile, ecc.)
4) **Checklist rischi/compatibilità**:
   - sicurezza endpoint email (validation/anti-spam basilare)
   - fallback testo email
   - gestione lingua IT/EN se pertinente alle pagine (solo se già presente nel progetto)
5) **Regole operative**:
   - Proporre uno step per volta: dopo l’analisi mi farai scegliere lo step 1 e poi si procede iterativamente.

## Importante
Non inventare funzionalità non presenti nel MD.
Non cambiare UX esistente fuori da Eventi Privati.
Non toccare pagine pubbliche già live, se non strettamente necessario.

FINE — Ora analizza MD + repo e rispondi con il piano.
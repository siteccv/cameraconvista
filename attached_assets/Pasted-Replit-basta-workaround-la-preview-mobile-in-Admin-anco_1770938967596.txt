Replit, basta workaround: la preview mobile in Admin è ancora sbagliata e ci stiamo perdendo troppo tempo.

Situazione attuale (non accettabile)

Per vedere una preview “realistica” devo zoomare il browser.

Con lo zoom, la pagina admin diventa ingestibile.

La preview mobile cambia proporzioni e contenuto in base a:

zoom del browser

dimensione finestra

proporzioni finestra

Questa non è una preview: è una finestra ridotta.

Obiettivo (non negoziabile)

Voglio una device preview con le stesse proporzioni di un iPhone reale (riferimento: iPhone 15 Pro), che sia:

FISSA e RIPETIBILE

dimensioni/proporzioni del “display” devono rimanere identiche sempre

non deve mai “rispondere” alla finestra del browser

INDIPENDENTE dallo zoom del browser

se l’admin fa zoom + / zoom –:
✅ il “telefono” può diventare più grande o più piccolo come elemento grafico
❌ ma il contenuto dentro deve restare invariato (stessa resa, stessa scala interna, stessa impaginazione)

Standard “da software serio”

un frame device con viewport reale (non una pagina che si adatta)

se lo spazio non basta: si adatta l’area attorno (scroll/margini), NON il telefono

Richieste operative (ordine obbligatorio)

Analisi e diagnosi: individua esattamente perché oggi la preview non è device-locked (vh, breakpoints reali, scaling, iframe, ecc.).

Proposta A (fix): soluzione minima per rendere la preview realmente “device preview” fissa.

Proposta B (refactor/rifare): se fixarla richiede patch sporche o compromessi, proponi di rifarla pulita.

Implementa solo dopo mia conferma.

Vincoli fondamentali

NON toccare le logiche già concluse fino ad ora:

ImageContainer

referenceWidth / crop stabile desktop

layout contenuto e margini

editing desktop/mobile indipendente

Questa task riguarda solo la cornice/preview mobile in Admin.

Output richiesto

Un report MD in report/ con:

diagnosi tecnica (causa reale)

Proposta A (fix) + rischi

Proposta B (rifare) + rischi

raccomandazione finale tua (motivata)

checklist test di accettazione (chiari e ripetibili)

Poi mi chiedi “A o B” e procediamo.
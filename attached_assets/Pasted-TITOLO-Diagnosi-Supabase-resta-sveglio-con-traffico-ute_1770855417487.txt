TITOLO
Diagnosi “Supabase resta sveglio con traffico utenti?” + verifica fetch reali (Report in chat)

CONTESTO
Ho un sito/app (Camera con Vista) deployato su Render FREE e database su Supabase FREE.
Render va in sleep dopo ~15 minuti di inattività. Supabase può andare in pausa dopo ~7 giorni senza attività.
Voglio capire se le azioni reali degli utenti sul sito (navigazione, click “Prenota un tavolo”, apertura pagine) generano davvero attività su Supabase e quindi impediscono lo standby, oppure se Supabase resta inattivo perché i contenuti sono statici o non fanno query.

RICHIESTA (SOLO DIAGNOSI — NO FIX)
1) Mappa i flussi “pubblici” principali del sito e indica, per ciascuno, se genera chiamate al backend e/o query su Supabase:
   - Home
   - Menu
   - Carta vini / Lista vini
   - Cocktail bar
   - Eventi
   - Contatti
   - Click “Prenota un tavolo” (specifica se è solo link esterno o se passa da API)
   Per ogni voce scrivi: “SI muove Supabase” / “NO non muove Supabase” e spiega in 1 riga perché (es: “fa GET /api/pages → Supabase select”, oppure “solo link esterno”).

2) Individua nel codice dove avvengono i fetch dei dati:
   - Endpoint server chiamati dal client (es. /api/…)
   - Moduli/servizi che interrogano Supabase (supabaseAdmin / supabasePublic / SupabaseStorage)
   - Elenca le tabelle interrogate nelle pagine pubbliche (se possibile).

3) Verifica se la parte pubblica usa dati:
   - Sempre dinamici (fetch a ogni caricamento)
   - Cache lato server/client (React Query ecc.)
   - Statici (hardcoded / build-time)
   Specifica cosa succede su primo load e su refresh.

4) Verifica “sufficienza” rispetto allo standby Supabase:
   - Se c’è traffico medio (anche basso), è realistico che Supabase resti attivo?
   - Ci sono pagine che NON fanno alcuna query e quindi non contribuiscono?
   - La query avviene via REST (supabase-js/PostgREST) o via connessione Postgres diretta (pg)?
   (Non implementare nulla, solo rilevazione.)

5) Output richiesto (REPORT IN CHAT)
Produci un report strutturato con:
- Tabella/Elenco per pagina-azione → “muove Supabase? sì/no” + endpoint coinvolti
- Lista endpoint /api chiamati dal client in area pubblica
- Lista query/tabelle Supabase effettivamente toccate dal pubblico
- Conclusione chiara: “Con traffico utenti, Supabase rimane attivo?” (SÌ/NO/DIPENDE) + motivazione

VINCOLI
- Nessuna modifica al codice.
- Nessun refactor.
- Nessun nuovo file.
- Solo analisi e report in chat.
